#include <Servo.h>

Servo servo; 

const int potPin = A0; 
void setup() {
  servo.attach(9);

void loop() {
  int potValue = analogRead(potPin);
  
  int angle = map(potValue, 0, 1023, 0, 180);
  
  servo.write(angle); 
  
  delay(15); 
}
